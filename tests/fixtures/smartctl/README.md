Real `smartctl -j` outputs from the Prometheus smartctl_exporter project
(https://github.com/prometheus-community/smartctl_exporter, `testdata/`,
Apache License 2.0). Serial numbers were already redacted upstream.
